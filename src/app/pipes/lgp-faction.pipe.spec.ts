import { LgpFactionPipe } from './lgp-faction.pipe';

describe('LgpFactionPipe', () => {
  it('create an instance', () => {
    const pipe = new LgpFactionPipe();
    expect(pipe).toBeTruthy();
  });
});
