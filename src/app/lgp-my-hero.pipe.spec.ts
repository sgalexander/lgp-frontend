import { LgpMyHeroPipe } from './lgp-my-hero.pipe';

describe('LgpMyHeroPipe', () => {
  it('create an instance', () => {
    const pipe = new LgpMyHeroPipe();
    expect(pipe).toBeTruthy();
  });
});
